# Elastic Beanstalk Application Source Uploader

This function runs when the stack is deployed and uploads a pre-built version of the webapi project to the S3 Bucket used for the source bundle artifacts.